import { checkForName } from '../src/client/js/nameChecker'

describe("Testing the checkForName function", () => {
  test("Testing if checkForName function exist", () => {
    expect(checkForName).toBeDefined();
  })
})
